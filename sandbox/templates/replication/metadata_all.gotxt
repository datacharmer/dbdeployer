#!{{.ShellPath}}
{{.Copyright}}
# Generated by dbdeployer {{.AppVersion}} using {{.TemplateName}} on {{.DateTime}}
SBDIR={{.SandboxDir}}
if [ "$1" = "" ]
then
  echo "syntax: $0 keyword"
  exit 1
fi

	echo "# {{.MasterLabel}}  "
	$SBDIR/{{.MasterLabel}}/metadata $@

{{range .Slaves}}
	echo "# server: {{.Node}} "
	$SBDIR/{{.NodeLabel}}{{.Node}}/metadata $@
{{end}}
