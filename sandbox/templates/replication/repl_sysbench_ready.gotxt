#!{{.ShellPath}}
{{.Copyright}}
# Generated by dbdeployer {{.AppVersion}} using {{.TemplateName}} on {{.DateTime}}
SBDIR={{.SandboxDir}}

sysbench_ready=$SBDIR/{{.MasterLabel}}/sysbench_ready

if [ ! -x $sysbench_ready ]
then
    echo "$sysbench_ready not found"
    exit 1
fi

$sysbench_ready $@
