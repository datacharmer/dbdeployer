#!{{.ShellPath}}
{{.Copyright}}
# Generated by dbdeployer {{.AppVersion}} using {{.TemplateName}} on {{.DateTime}}
SBDIR={{.SandboxDir}}
cd "$SBDIR"

MASTERS="{{.MasterList}}"
SLAVES="{{.SlaveList}}"

for N in $MASTERS
do
	echo "# server: $N"
	cd "$SBDIR/{{.NodeLabel}}$N"
    $@
    cd - > /dev/null
done
